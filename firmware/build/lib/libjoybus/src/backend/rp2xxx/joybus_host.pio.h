// ---------------------------------------------------------------- //
// This file is autogenerated by pioasm version 2.2.0; do not edit! //
// ---------------------------------------------------------------- //

#pragma once

#if !PICO_NO_HARDWARE
#include "hardware/pio.h"
#endif

// ----------- //
// joybus_host //
// ----------- //

#define joybus_host_wrap_target 0
#define joybus_host_wrap 14
#define joybus_host_pio_version 0

#define joybus_host_TICKS_PER_BIT 24

#define joybus_host_offset_transmit 0u

static const uint16_t joybus_host_program_instructions[] = {
            //     .wrap_target
    0x80a0, //  0: pull   block           side 0
    0x7021, //  1: out    x, 1            side 1
    0x1424, //  2: jmp    !x, 4           side 1 [4]
    0x0b05, //  3: jmp    5               side 0 [11]
    0xbb42, //  4: nop                    side 1 [11]
    0x05e1, //  5: jmp    !osre, 1        side 0 [5]
    0xb542, //  6: nop                    side 1 [5]
    0xc010, //  7: irq    nowait 0 rel    side 0
    0xe027, //  8: set    x, 7            side 0
    0x20a0, //  9: wait   1 pin, 0        side 0
    0x2820, // 10: wait   0 pin, 0        side 0 [8]
    0x4001, // 11: in     pins, 1         side 0
    0x0049, // 12: jmp    x--, 9          side 0
    0xc010, // 13: irq    nowait 0 rel    side 0
    0x0008, // 14: jmp    8               side 0
            //     .wrap
};

#if !PICO_NO_HARDWARE
static const struct pio_program joybus_host_program = {
    .instructions = joybus_host_program_instructions,
    .length = 15,
    .origin = -1,
    .pio_version = joybus_host_pio_version,
#if PICO_PIO_VERSION > 0
    .used_gpio_ranges = 0x0
#endif
};

static inline pio_sm_config joybus_host_program_get_default_config(uint offset) {
    pio_sm_config c = pio_get_default_sm_config();
    sm_config_set_wrap(&c, offset + joybus_host_wrap_target, offset + joybus_host_wrap);
    sm_config_set_sideset(&c, 1, false, true);
    return c;
}

#include "hardware/clocks.h"
static inline void joybus_host_program_init(PIO pio, uint sm, uint offset, uint pin, uint bus_freq) {
  pio_sm_config c = joybus_host_program_get_default_config(offset);
  // Set pin configuration
  sm_config_set_in_pins(&c, pin);
  sm_config_set_sideset_pins(&c, pin);
  // Auto-pull and auto-push with 8-bit thresholds
  sm_config_set_out_shift(&c, false, true, 8);
  sm_config_set_in_shift(&c, false, true, 8);
  // Set clock divider based on desired bus frequency and internal timing
  float div = (float)clock_get_hz(clk_sys) / (joybus_host_TICKS_PER_BIT * bus_freq);
  sm_config_set_clkdiv(&c, div);
  // Initialize and enable the state machine
  pio_sm_init(pio, sm, offset, &c);
  pio_sm_set_enabled(pio, sm, true);
}

#endif

